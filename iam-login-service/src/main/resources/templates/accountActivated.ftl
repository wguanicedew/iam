Dear ${recipient},

your registration request has been approved.

The username for your account is:

${username}

You can set your local $organisationName password by following this link:

${resetPasswordUrl}

Note that this step is not needed if you are using an external identity provider
to log into ${organisationName}, your account is already active.

The ${organisationName} registration service